import React from 'react';
import RegisterForm from '../components/auth/RegisterForm';

// fixed the Register Page 
const RegisterPage: React.FC = () => {
  return <RegisterForm />;
};

export default RegisterPage;
